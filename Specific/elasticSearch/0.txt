node client and transport client over port 9300

REST API on 9200

document belongs to a type, and types live inside an index

HEAD verb to check whether the doucment exists

GET /megacorp/employee/_search?q=last_name:Smith

or pass DSL json to _search

documents are immutable, version number based, marked for deletion and then recycled

optimistic concurrency control

common setup is to use other db as primary,a dn ES to make data searchable

Q: field collapsing

----

Type represents a class of similar documents, with a name and a mapping, which describs fiels/properits that docuemnt of taht type may have

when index  docu in lucne, the value s for each field are adde do the inverted index for the assoicated field
