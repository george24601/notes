final class => can not be subclassed

final method => can not be overriden (member methods) or hidden(static methods) by subclasses

blank final => must be assigned in every ctor of the class, or if it is static, the static initializer of the class

java annotations can be reflective that can be embedded in class files generated by the compiler

How to implement a custom annotation and use it?

