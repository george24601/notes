Sequenct file
flat file containing k/v pairs. Used in map reduce as input/output formats

can read/write/sort

3 formats:
uncopressed k/v records
record compression: values compressed
block compression: both k and v are clooected in blocks separately and compressed

Sequence file starts with a header, regardless of format

-----
Text file

------
To be added: binary object file

----
parquet file (SparkSQL only)

