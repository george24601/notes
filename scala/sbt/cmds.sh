#run sbt in batch mode. testOnly is the cmd with 2 args testA and testB
sbt clean compile "testOnly TestA TestB"

#run the main class in the same VM as sbt
run args*

#create a jar file containing files in src/main/resources and src/main/scala
package

#reload build definition
reload

#history commands

#displays (and parses) scoped key
{<build-uri>}<project-id>/config:intask::key
